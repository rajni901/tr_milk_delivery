<?xml version="1.0" encoding="utf-8"?>
<odoo>

<template id="driver_portal_template" name="Driver Delivery App">
<t t-call="web.layout">
<t t-set="additional_head">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1"/>
    <meta name="theme-color" content="#1a1a5e"/>
    <style>
        *{box-sizing:border-box;margin:0;padding:0}
        body{font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,sans-serif;background:#f0f2f5;min-height:100vh}
        .dh{background:linear-gradient(135deg,#1a1a5e 0%,#6c3fc5 100%);padding:16px 20px;position:sticky;top:0;z-index:99;box-shadow:0 4px 12px rgba(0,0,0,.3)}
        .dh-title{color:#fff;font-size:20px;font-weight:900;display:flex;align-items:center;gap:8px}
        .dh-sub{color:rgba(255,255,255,.8);font-size:12px;margin-top:4px}
        .prog-wrap{background:#fff;padding:14px 16px;border-bottom:1px solid #e8eaed}
        .prog-label{display:flex;justify-content:space-between;font-size:13px;font-weight:600;margin-bottom:6px}
        .prog-count{color:#28a745;font-weight:800}
        .prog-bar{background:#e9ecef;border-radius:10px;height:10px}
        .prog-fill{background:linear-gradient(90deg,#28a745,#20c997);height:10px;border-radius:10px;transition:width .4s}
        .card{background:#fff;margin:12px;border-radius:16px;box-shadow:0 2px 12px rgba(0,0,0,.09);overflow:hidden}
        .card-pending{border-left:5px solid #6c3fc5}
        .card-delivered{border-left:5px solid #28a745;opacity:.8}
        .card-skipped{border-left:5px solid #ffc107;opacity:.8}
        .card-absent{border-left:5px solid #dc3545;opacity:.8}
        .card-top{padding:14px 16px 8px;display:flex;align-items:flex-start;gap:12px}
        .stop-badge{min-width:36px;height:36px;background:linear-gradient(135deg,#1a1a5e,#6c3fc5);color:#fff;border-radius:50%;display:flex;align-items:center;justify-content:center;font-weight:900;font-size:13px;flex-shrink:0}
        .cust-name{font-size:17px;font-weight:800;color:#1a1a5e;line-height:1.2}
        .cust-addr{font-size:12px;color:#888;margin-top:2px}
        .status-pill{margin-left:auto;flex-shrink:0;font-size:11px;font-weight:700;padding:4px 12px;border-radius:20px}
        .sp-pending{background:#ede8ff;color:#5a2fc2}
        .sp-delivered{background:#d4edda;color:#155724}
        .sp-skipped{background:#fff3cd;color:#856404}
        .sp-absent{background:#f8d7da;color:#721c24}
        .prod-row{padding:2px 16px 10px 64px;font-size:13px;color:#555}
        .prod-row strong{color:#333}
        .note-row{margin:0 16px 10px;background:#fffde7;border-radius:8px;padding:8px 12px;font-size:12px;color:#6d5c00;border-left:3px solid #ffc107}
        .actions{display:flex;gap:8px;padding:8px 16px 16px}
        .btn-d{flex:2;background:#28a745;color:#fff;border:none;border-radius:12px;padding:14px 10px;font-size:15px;font-weight:700;cursor:pointer;box-shadow:0 3px 8px rgba(40,167,69,.4)}
        .btn-s{flex:1;background:#fff3cd;color:#856404;border:2px solid #ffc107;border-radius:12px;padding:14px 6px;font-size:13px;font-weight:700;cursor:pointer}
        .btn-a{flex:1;background:#f8d7da;color:#721c24;border:2px solid #dc3545;border-radius:12px;padding:14px 6px;font-size:13px;font-weight:700;cursor:pointer}
        .btn-undo{width:100%;background:#f8f9fa;color:#6c757d;border:1px solid #dee2e6;border-radius:12px;padding:11px;font-size:13px;cursor:pointer;margin:0 16px 16px;width:calc(100% - 32px)}
        .empty{text-align:center;padding:80px 30px;color:#888}
        .empty-icon{font-size:56px;margin-bottom:12px}
        .empty h3{font-size:20px;color:#555;margin-bottom:8px}
        .done-card{background:#fff;margin:12px;border-radius:16px;padding:24px;text-align:center;box-shadow:0 2px 12px rgba(0,0,0,.09)}
        .done-icon{font-size:56px}
        .done-title{color:#28a745;font-size:22px;font-weight:900;margin:10px 0 6px}
        .spacer{height:48px}
        form{display:contents}
    </style>
</t>

<!-- Header -->
<div class="dh">
    <div class="dh-title">🥛 <t t-esc="route.name"/></div>
    <div class="dh-sub">
        👤 <t t-esc="route.driver_id.name"/> &#160;·&#160; 📅 <t t-esc="today"/>
    </div>
</div>

<!-- Progress -->
<div class="prog-wrap">
    <div class="prog-label">
        <span>Deliveries Progress</span>
        <span class="prog-count"><t t-esc="delivered"/>/<t t-esc="total"/> Done</span>
    </div>
    <div class="prog-bar">
        <div class="prog-fill"
             t-att-style="'width:' + (str(int(delivered * 100 / total)) if total else '0') + '%'"/>
    </div>
</div>

<!-- No sheet -->
<t t-if="not sheet or total == 0">
    <div class="empty">
        <div class="empty-icon">📋</div>
        <h3>No Deliveries Today</h3>
        <p>No delivery sheet found for today.<br/>Ask the manager to generate it.</p>
    </div>
</t>

<!-- All done celebration -->
<t t-if="sheet and total > 0 and delivered == total">
    <div class="done-card">
        <div class="done-icon">🎉</div>
        <div class="done-title">Route Complete!</div>
        <p style="color:#666;">All <strong><t t-esc="total"/></strong> deliveries done.</p>
    </div>
</t>

<!-- Delivery cards -->
<t t-foreach="deliveries" t-as="d">
    <div t-att-class="'card card-' + d.state">

        <!-- Top row: stop badge + name + status pill -->
        <div class="card-top">
            <div class="stop-badge"><t t-esc="d_index + 1"/></div>
            <div style="flex:1;">
                <div class="cust-name"><t t-esc="d.partner_id.name"/></div>
                <div class="cust-addr">
                    <t t-if="d.partner_id.street">
                        <t t-esc="d.partner_id.street"/>
                    </t>
                </div>
            </div>
            <span t-att-class="'status-pill sp-' + d.state">
                <t t-if="d.state == 'delivered'">✓ Done</t>
                <t t-elif="d.state == 'skipped'">Skipped</t>
                <t t-elif="d.state == 'absent'">Absent</t>
                <t t-else="">Pending</t>
            </span>
        </div>

        <!-- Product row -->
        <div class="prod-row">
            📦 <t t-esc="d.product_id.name"/> · Qty:
            <strong><t t-esc="d.qty"/> <t t-esc="d.product_id.uom_id.name"/></strong>
            · <strong>
                <t t-esc="d.subtotal" t-options="{'widget': 'float', 'precision': 2}"/>
            </strong>
        </div>

        <!-- Delivery note -->
        <t t-if="d.subscription_id and d.subscription_id.note">
            <div class="note-row">📍 <t t-esc="d.subscription_id.note"/></div>
        </t>

        <!-- Action buttons — only for pending -->
        <t t-if="d.state == 'pending'">
            <div class="actions">
                <form t-att-action="'/milk/driver/' + token + '/action'" method="POST">
                    <input type="hidden" name="delivery_id" t-att-value="str(d.id)"/>
                    <input type="hidden" name="action" value="deliver"/>
                    <button type="submit" class="btn-d">✓ Delivered</button>
                </form>
                <form t-att-action="'/milk/driver/' + token + '/action'" method="POST">
                    <input type="hidden" name="delivery_id" t-att-value="str(d.id)"/>
                    <input type="hidden" name="action" value="skip"/>
                    <button type="submit" class="btn-s">Skip</button>
                </form>
                <form t-att-action="'/milk/driver/' + token + '/action'" method="POST">
                    <input type="hidden" name="delivery_id" t-att-value="str(d.id)"/>
                    <input type="hidden" name="action" value="absent"/>
                    <button type="submit" class="btn-a">Absent</button>
                </form>
            </div>
        </t>

        <!-- Undo button — for completed/skipped/absent -->
        <t t-if="d.state != 'pending'">
            <form t-att-action="'/milk/driver/' + token + '/action'" method="POST">
                <input type="hidden" name="delivery_id" t-att-value="str(d.id)"/>
                <input type="hidden" name="action" value="reset"/>
                <button type="submit" class="btn-undo">↩ Undo</button>
            </form>
        </t>

    </div>
</t>

<div class="spacer"/>
</t>
</template>

<!-- Invalid token -->
<template id="driver_portal_invalid" name="Driver Portal Invalid">
<t t-call="web.layout">
    <t t-set="additional_head">
        <style>body{font-family:Arial,sans-serif;background:#f0f2f5;display:flex;align-items:center;justify-content:center;min-height:100vh;text-align:center}</style>
    </t>
    <div style="padding:40px;">
        <div style="font-size:64px;">🔒</div>
        <h2 style="color:#dc3545;margin:16px 0 8px;">Invalid Driver Link</h2>
        <p style="color:#666;">This link is invalid or has been reset.<br/>Ask your manager for a new link.</p>
    </div>
</t>
</template>

</odoo>
